{
  "name": "jackman",
  "author": "Michal Bambuch",
  "license": "MIT",
  "dependencies": {
    "pixi.js": "^5.3.7"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "build": "node scripts/prebuild.js && parcel build index.html --out-dir build",
    "dev": "node scripts/prebuild.js && parcel index.html --out-dir build",
    "clean": "rm -rf node_modules && rm -rf build && rm -rf .cache"
  }
}
